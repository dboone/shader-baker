<UserControl x:Class="ShaderBaker.View.ShaderTextView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ShaderBaker.View"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <DockPanel HorizontalAlignment="Stretch" VerticalAlignment="Stretch" >
        <TextBox Grid.Column="0" AcceptsReturn="True" TextWrapping="Wrap" FontFamily="Courier New" Background="#FF222222" Foreground="#FFFAEBD7" FontSize="14" Text="{Binding Text}"/>
    </DockPanel>
</UserControl>
